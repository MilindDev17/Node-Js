const fs = require('fs');
const os = require('os');
const notes = require('./notes');

const _ = require('lodsh');
const yargs = require('yargs');

const argv = yargs.options({
	help = 	 

})  

//console.log(notes.add(9,2));

if(command == 'add'){
	console.log('hi');
}

//var users = os.userInfo();
// fs.appendFile('textCretefs.txt',`Hello test first one ${users.username}`);
